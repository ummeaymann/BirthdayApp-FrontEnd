import { AnniversaryPipe } from './anniversary.pipe';

describe('AnniversaryPipe', () => {
  it('create an instance', () => {
    const pipe = new AnniversaryPipe();
    expect(pipe).toBeTruthy();
  });
});
